# Firebase config for stg

Copy the staging Firebase config to this directory with this exact path:

`app/src/stg/google-services.json`

The Firebase Android app must use package name `tmh.nhoctax.githubusers.stg`.
