# Firebase config for dev

Copy the development Firebase config to this directory with this exact path:

`app/src/dev/google-services.json`

The Firebase Android app must use package name `tmh.nhoctax.githubusers.dev`.
