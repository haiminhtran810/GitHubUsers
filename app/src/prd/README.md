# Firebase config for prd

Copy the production Firebase config to this directory with this exact path:

`app/src/prd/google-services.json`

The Firebase Android app must use package name `tmh.nhoctax.githubusers`.
