package tmh.nhoctax.githubusers.core.navigation

object AppDeepLinks {
    private const val HOST = "https://nhoctax.com"
    const val USER_LIST_PATH = "$HOST/users"
    const val USER_DETAILS_PATH = "$HOST/users/{username}"
    const val FAVORITE_PATH = "$HOST/favorite"
}
